// Auto-generated by blueberry-generator-typescript. Do not edit.

/* eslint-disable */
/* prettier-ignore */

import {
  MessageHeader,
  deserializeMessage,
  serializeMessage,
} from '../runtime.js';

export type FloatSequence = number[];

export enum PortPinEnum {
  GPIO_A0_PIN = 0,
  GPIO_A1_PIN = 1,
  GPIO_A2_PIN = 2,
  GPIO_A3_PIN = 3,
  GPIO_A4_PIN = 4,
  GPIO_A5_PIN = 5,
  GPIO_A6_PIN = 6,
  GPIO_A7_PIN = 7,
  GPIO_A8_PIN = 8,
  GPIO_A9_PIN = 9,
  GPIO_A10_PIN = 10,
  GPIO_A11_PIN = 11,
  GPIO_A12_PIN = 12,
  GPIO_A13_PIN = 13,
  GPIO_A14_PIN = 14,
  GPIO_A15_PIN = 15,
  GPIO_B0_PIN = 16,
  GPIO_B1_PIN = 17,
  GPIO_B2_PIN = 18,
  GPIO_B3_PIN = 19,
  GPIO_B4_PIN = 20,
  GPIO_B5_PIN = 21,
  GPIO_B6_PIN = 22,
  GPIO_B7_PIN = 23,
  GPIO_B8_PIN = 24,
  GPIO_B9_PIN = 25,
  GPIO_B10_PIN = 26,
  GPIO_B11_PIN = 27,
  GPIO_B12_PIN = 28,
  GPIO_B13_PIN = 29,
  GPIO_B14_PIN = 30,
  GPIO_B15_PIN = 31,
  GPIO_C0_PIN = 32,
  GPIO_C1_PIN = 33,
  GPIO_C2_PIN = 34,
  GPIO_C3_PIN = 35,
  GPIO_C4_PIN = 36,
  GPIO_C5_PIN = 37,
  GPIO_C6_PIN = 38,
  GPIO_C7_PIN = 39,
  GPIO_C8_PIN = 40,
  GPIO_C9_PIN = 41,
  GPIO_C10_PIN = 42,
  GPIO_C11_PIN = 43,
  GPIO_C12_PIN = 44,
  GPIO_C13_PIN = 45,
  GPIO_C14_PIN = 46,
  GPIO_C15_PIN = 47,
  GPIO_D0_PIN = 48,
  GPIO_D1_PIN = 49,
  GPIO_D2_PIN = 50,
  GPIO_D3_PIN = 51,
  GPIO_D4_PIN = 52,
  GPIO_D5_PIN = 53,
  GPIO_D6_PIN = 54,
  GPIO_D7_PIN = 55,
  GPIO_D8_PIN = 56,
  GPIO_D9_PIN = 57,
  GPIO_D10_PIN = 58,
  GPIO_D11_PIN = 59,
  GPIO_D12_PIN = 60,
  GPIO_D13_PIN = 61,
  GPIO_D14_PIN = 62,
  GPIO_D15_PIN = 63,
  GPIO_E0_PIN = 64,
  GPIO_E1_PIN = 65,
  GPIO_E2_PIN = 66,
  GPIO_E3_PIN = 67,
  GPIO_E4_PIN = 68,
  GPIO_E5_PIN = 69,
  GPIO_E6_PIN = 70,
  GPIO_E7_PIN = 71,
  GPIO_E8_PIN = 72,
  GPIO_E9_PIN = 73,
  GPIO_E10_PIN = 74,
  GPIO_E11_PIN = 75,
  GPIO_E12_PIN = 76,
  GPIO_E13_PIN = 77,
  GPIO_E14_PIN = 78,
  GPIO_E15_PIN = 79,
  GPIO_F0_PIN = 80,
  GPIO_F1_PIN = 81,
  GPIO_F2_PIN = 82,
  GPIO_F3_PIN = 83,
  GPIO_F4_PIN = 84,
  GPIO_F5_PIN = 85,
  GPIO_F6_PIN = 86,
  GPIO_F7_PIN = 87,
  GPIO_F8_PIN = 88,
  GPIO_F9_PIN = 89,
  GPIO_F10_PIN = 90,
  GPIO_F11_PIN = 91,
  GPIO_F12_PIN = 92,
  GPIO_F13_PIN = 93,
  GPIO_F14_PIN = 94,
  GPIO_F15_PIN = 95,
  GPIO_G0_PIN = 96,
  GPIO_G1_PIN = 97,
  GPIO_G2_PIN = 98,
  GPIO_G3_PIN = 99,
  GPIO_G4_PIN = 100,
  GPIO_G5_PIN = 101,
  GPIO_G6_PIN = 102,
  GPIO_G7_PIN = 103,
  GPIO_G8_PIN = 104,
  GPIO_G9_PIN = 105,
  GPIO_G10_PIN = 106,
  GPIO_G11_PIN = 107,
  GPIO_G12_PIN = 108,
  GPIO_G13_PIN = 109,
  GPIO_G14_PIN = 110,
  GPIO_G15_PIN = 111,
  GPIO_H0_PIN = 112,
  GPIO_H1_PIN = 113,
  GPIO_H2_PIN = 114,
  GPIO_H3_PIN = 115,
  GPIO_H4_PIN = 116,
  GPIO_H5_PIN = 117,
  GPIO_H6_PIN = 118,
  GPIO_H7_PIN = 119,
  GPIO_H8_PIN = 120,
  GPIO_H9_PIN = 121,
  GPIO_H10_PIN = 122,
  GPIO_H11_PIN = 123,
  GPIO_H12_PIN = 124,
  GPIO_H13_PIN = 125,
  GPIO_H14_PIN = 126,
  GPIO_H15_PIN = 127,
  GPIO_I0_PIN = 128,
  GPIO_I1_PIN = 129,
  GPIO_I2_PIN = 130,
  GPIO_I3_PIN = 131,
  GPIO_I4_PIN = 132,
  GPIO_I5_PIN = 133,
  GPIO_I6_PIN = 134,
  GPIO_I7_PIN = 135,
  GPIO_I8_PIN = 136,
  GPIO_I9_PIN = 137,
  GPIO_I10_PIN = 138,
  GPIO_I11_PIN = 139,
  GPIO_I12_PIN = 140,
  GPIO_I13_PIN = 141,
  GPIO_I14_PIN = 142,
  GPIO_I15_PIN = 143,
  GPIO_NULL_PIN = 255,
}

export interface AdcConfigStruct {
  channel: number;
  enabled: boolean;
  timeConstantSeconds: number;
  gain: number;
  offset: number;
}

export type AdcConfigSequence = AdcConfigStruct[];

export const ADC_CONFIG_TOPIC = 'blueberry/devices/{device_type}/{nid}/adc-config';

export interface AdcConfigFields {
  config: AdcConfigStruct[];
}

export class AdcConfigMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xf7f2;
  static readonly TOPIC_TEMPLATE = ADC_CONFIG_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = AdcConfigMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: AdcConfigFields): Uint8Array {
    return serializeMessage(
      fields,
      AdcConfigMessage.MODULE_KEY,
      AdcConfigMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fconfig = w.beginSequence();
          for (const elem_fconfig of f.config ?? []) {
            seq_fconfig.writeElement((sw) => {
              {
                sw.writeU8(elem_fconfig.channel);
                sw.writeBool(elem_fconfig.enabled);
                sw.writeF32(elem_fconfig.timeConstantSeconds);
                sw.writeF32(elem_fconfig.gain);
                sw.writeF32(elem_fconfig.offset);
              }
            });
          }
          seq_fconfig.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: AdcConfigFields } {
    return deserializeMessage<AdcConfigFields>(bytes, (r) => ({
      config: (() => {
        const seq = r.beginSequence();
        const out: AdcConfigStruct[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(
            seq.readElement((sr) => ({
              channel: sr.readU8(),
              enabled: sr.readBool(),
              timeConstantSeconds: sr.readF32(),
              gain: sr.readF32(),
              offset: sr.readF32(),
            })),
          );
        }
        return out;
      })(),
    }));
  }
}

export interface AdcDataStruct {
  data: number;
}

export type AdcDataSequence = AdcDataStruct[];

export const ADC_DATA_TOPIC = 'blueberry/devices/{device_type}/{nid}/adc-data';

export interface AdcDataFields {
  data: AdcDataStruct[];
}

export class AdcDataMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xfc9d;
  static readonly TOPIC_TEMPLATE = ADC_DATA_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = AdcDataMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: AdcDataFields): Uint8Array {
    return serializeMessage(
      fields,
      AdcDataMessage.MODULE_KEY,
      AdcDataMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fdata = w.beginSequence();
          for (const elem_fdata of f.data ?? []) {
            seq_fdata.writeElement((sw) => {
              {
                sw.writeF32(elem_fdata.data);
              }
            });
          }
          seq_fdata.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: AdcDataFields } {
    return deserializeMessage<AdcDataFields>(bytes, (r) => ({
      data: (() => {
        const seq = r.beginSequence();
        const out: AdcDataStruct[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => ({ data: sr.readF32() })));
        }
        return out;
      })(),
    }));
  }
}

export const APP_DATA_TOPIC = 'blueberry/devices/{device_type}/{nid}/app-data';

export interface AppDataFields {
  floats: number[];
}

export class AppDataMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xdd4b;
  static readonly TOPIC_TEMPLATE = APP_DATA_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = AppDataMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: AppDataFields): Uint8Array {
    return serializeMessage(
      fields,
      AppDataMessage.MODULE_KEY,
      AppDataMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_ffloats = w.beginSequence();
          for (const elem_ffloats of f.floats ?? []) {
            seq_ffloats.writeElement((sw) => {
              sw.writeF32(elem_ffloats);
            });
          }
          seq_ffloats.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: AppDataFields } {
    return deserializeMessage<AppDataFields>(bytes, (r) => ({
      floats: (() => {
        const seq = r.beginSequence();
        const out: number[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => sr.readF32()));
        }
        return out;
      })(),
    }));
  }
}

export interface FeedbackTrajectoryStruct {
  channel: number;
  setpoint: number;
  setpointRate: number;
  time: number;
}

export type TrajectorySequence = FeedbackTrajectoryStruct[];

export interface FeedbackResponseStruct {
  channel: number;
  queueLength: number;
  actual: number;
  setpoint: number;
  output: number;
  endTime: number;
}

export type ResponseSequence = FeedbackResponseStruct[];

export enum Unit {
  METRES = 0,
  RADIANS = 1,
  METRES_PER_SECOND = 3,
  RADIANS_PER_SECOND = 4,
  DEGREES = 5,
  METRES_PER_SECOND_SQUARED = 6,
  RADIANS_PER_SECOND_SQUARED = 7,
  NEWTONS = 8,
  NEWTON_METRES = 9,
}

export enum PlumbingType {
  UNCONFIGURED = 0,
  OPEN_LOOP = 1,
  TX_APP_DATA = 2,
  RX_APP_DATA = 3,
  STREAM = 4,
}

export interface FeedbackConfigStruct {
  channel: number;
  unit: Unit;
  enable: boolean;
  inputType: PlumbingType;
  minInput: number;
  maxInput: number;
  minOutput: number;
  maxOutput: number;
  outputType: PlumbingType;
  inputChannel: number;
  outputChannel: number;
  c0: number;
  c1: number;
  c2: number;
  d1: number;
  d2: number;
}

export type ConfigSequence = FeedbackConfigStruct[];

export const FEEDBACK_TOPIC = 'blueberry/devices/{device_type}/{nid}/feedback';

export interface FeedbackFields {
  config: FeedbackConfigStruct[];
  trajectories: FeedbackTrajectoryStruct[];
  response: FeedbackResponseStruct[];
}

export class FeedbackMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x4d78;
  static readonly TOPIC_TEMPLATE = FEEDBACK_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = FeedbackMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: FeedbackFields): Uint8Array {
    return serializeMessage(
      fields,
      FeedbackMessage.MODULE_KEY,
      FeedbackMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fconfig = w.beginSequence();
          for (const elem_fconfig of f.config ?? []) {
            seq_fconfig.writeElement((sw) => {
              {
                sw.writeU8(elem_fconfig.channel);
                sw.writeU8(elem_fconfig.unit as number);
                sw.writeBool(elem_fconfig.enable);
                sw.writeU8(elem_fconfig.inputType as number);
                sw.writeF32(elem_fconfig.minInput);
                sw.writeF32(elem_fconfig.maxInput);
                sw.writeF32(elem_fconfig.minOutput);
                sw.writeF32(elem_fconfig.maxOutput);
                sw.writeU8(elem_fconfig.outputType as number);
                sw.writeU8(elem_fconfig.inputChannel);
                sw.writeU8(elem_fconfig.outputChannel);
                sw.writeF32(elem_fconfig.c0);
                sw.writeF32(elem_fconfig.c1);
                sw.writeF32(elem_fconfig.c2);
                sw.writeF32(elem_fconfig.d1);
                sw.writeF32(elem_fconfig.d2);
              }
            });
          }
          seq_fconfig.end();
        }
        {
          const seq_ftraject = w.beginSequence();
          for (const elem_ftraject of f.trajectories ?? []) {
            seq_ftraject.writeElement((sw) => {
              {
                sw.writeU8(elem_ftraject.channel);
                sw.writeF32(elem_ftraject.setpoint);
                sw.writeF32(elem_ftraject.setpointRate);
                sw.writeU32(elem_ftraject.time);
              }
            });
          }
          seq_ftraject.end();
        }
        {
          const seq_frespons = w.beginSequence();
          for (const elem_frespons of f.response ?? []) {
            seq_frespons.writeElement((sw) => {
              {
                sw.writeU8(elem_frespons.channel);
                sw.writeU8(elem_frespons.queueLength);
                sw.writeF32(elem_frespons.actual);
                sw.writeF32(elem_frespons.setpoint);
                sw.writeF32(elem_frespons.output);
                sw.writeU32(elem_frespons.endTime);
              }
            });
          }
          seq_frespons.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: FeedbackFields } {
    return deserializeMessage<FeedbackFields>(bytes, (r) => ({
      config: (() => {
        const seq = r.beginSequence();
        const out: FeedbackConfigStruct[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(
            seq.readElement((sr) => ({
              channel: sr.readU8(),
              unit: sr.readU8() as Unit,
              enable: sr.readBool(),
              inputType: sr.readU8() as PlumbingType,
              minInput: sr.readF32(),
              maxInput: sr.readF32(),
              minOutput: sr.readF32(),
              maxOutput: sr.readF32(),
              outputType: sr.readU8() as PlumbingType,
              inputChannel: sr.readU8(),
              outputChannel: sr.readU8(),
              c0: sr.readF32(),
              c1: sr.readF32(),
              c2: sr.readF32(),
              d1: sr.readF32(),
              d2: sr.readF32(),
            })),
          );
        }
        return out;
      })(),
      trajectories: (() => {
        const seq = r.beginSequence();
        const out: FeedbackTrajectoryStruct[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(
            seq.readElement((sr) => ({
              channel: sr.readU8(),
              setpoint: sr.readF32(),
              setpointRate: sr.readF32(),
              time: sr.readU32(),
            })),
          );
        }
        return out;
      })(),
      response: (() => {
        const seq = r.beginSequence();
        const out: FeedbackResponseStruct[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(
            seq.readElement((sr) => ({
              channel: sr.readU8(),
              queueLength: sr.readU8(),
              actual: sr.readF32(),
              setpoint: sr.readF32(),
              output: sr.readF32(),
              endTime: sr.readU32(),
            })),
          );
        }
        return out;
      })(),
    }));
  }
}

export enum FlashProgramState {
  DO_NOTHING_REQUEST = 0,
  UPLOAD_REQUEST = 1,
  RUN_REQUEST = 2,
  OVERWRITE_REQUEST = 3,
  CLEAR_REQUEST = 4,
  STOP_REQUEST = 5,
  IMAGE_GOOD_RESPONSE = 6,
  IMAGE_BAD_RESPONSE = 7,
  IMAGE_CLEAR_RESPONSE = 8,
  UPLOADING_RESPONSE = 9,
  UPLOAD_BAD_RESPONSE = 10,
  WILL_RUN_RESPONSE = 11,
  RUNNING_RESPONSE = 12,
  OVERWRITING_RESPONSE = 13,
  CLEARING_RESPONSE = 14,
}

export type FlashProgramSequence = number[];

export const FLASH_PROGRAM_TOPIC = 'blueberry/devices/{device_type}/{nid}/flash_program';

export interface FlashProgramFields {
  state: FlashProgramState;
  version: number;
  crc: number;
  start: number;
  end: number;
  count: number;
  data: number[];
}

export class FlashProgramMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xd12c;
  static readonly TOPIC_TEMPLATE = FLASH_PROGRAM_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = FlashProgramMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: FlashProgramFields): Uint8Array {
    return serializeMessage(
      fields,
      FlashProgramMessage.MODULE_KEY,
      FlashProgramMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeU8(f.state as number);
        w.writeU32(f.version);
        w.writeU32(f.crc);
        w.writeU32(f.start);
        w.writeU32(f.end);
        w.writeU32(f.count);
        {
          const seq_fdata = w.beginSequence();
          for (const elem_fdata of f.data ?? []) {
            seq_fdata.writeElement((sw) => {
              sw.writeU32(elem_fdata);
            });
          }
          seq_fdata.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: FlashProgramFields } {
    return deserializeMessage<FlashProgramFields>(bytes, (r) => ({
      state: r.readU8() as FlashProgramState,
      version: r.readU32(),
      crc: r.readU32(),
      start: r.readU32(),
      end: r.readU32(),
      count: r.readU32(),
      data: (() => {
        const seq = r.beginSequence();
        const out: number[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => sr.readU32()));
        }
        return out;
      })(),
    }));
  }
}

export enum GpioDir {
  INPUT = 0,
  OUTPUT = 1,
  ANALOG = 2,
  OUTPUT_LOW = 3,
  OUTPUT_HIGH = 4,
  AF0 = 5,
  AF1 = 6,
  AF2 = 7,
  AF3 = 8,
  AF4 = 9,
  AF5 = 10,
  AF6 = 11,
  AF7 = 12,
  AF8 = 13,
  AF9 = 14,
  AF10 = 15,
  AF11 = 16,
  AF12 = 17,
  AF13 = 18,
  AF14 = 19,
  AF15 = 20,
}

export interface GpioPinConfig {
  pin: number;
  dir: GpioDir;
}

export type GpioPinConfigSequence = GpioPinConfig[];

export const GPIO_CONFIG_TOPIC = 'blueberry/devices/{device_type}/{nid}/gpio-pin-config';

export interface GpioConfigFields {
  pinConfigs: GpioPinConfig[];
}

export class GpioConfigMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xd14e;
  static readonly TOPIC_TEMPLATE = GPIO_CONFIG_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = GpioConfigMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: GpioConfigFields): Uint8Array {
    return serializeMessage(
      fields,
      GpioConfigMessage.MODULE_KEY,
      GpioConfigMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fpinConf = w.beginSequence();
          for (const elem_fpinConf of f.pinConfigs ?? []) {
            seq_fpinConf.writeElement((sw) => {
              {
                sw.writeU8(elem_fpinConf.pin);
                sw.writeU8(elem_fpinConf.dir as number);
              }
            });
          }
          seq_fpinConf.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: GpioConfigFields } {
    return deserializeMessage<GpioConfigFields>(bytes, (r) => ({
      pinConfigs: (() => {
        const seq = r.beginSequence();
        const out: GpioPinConfig[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => ({ pin: sr.readU8(), dir: sr.readU8() as GpioDir })));
        }
        return out;
      })(),
    }));
  }
}

export type PortPins = number[];

export const GPIO_PINS_TOPIC = 'blueberry/devices/{device_type}/{nid}/gpio-pins';

export interface GpioPinsFields {
  port: number[];
}

export class GpioPinsMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x71ef;
  static readonly TOPIC_TEMPLATE = GPIO_PINS_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = GpioPinsMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: GpioPinsFields): Uint8Array {
    return serializeMessage(
      fields,
      GpioPinsMessage.MODULE_KEY,
      GpioPinsMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fport = w.beginSequence();
          for (const elem_fport of f.port ?? []) {
            seq_fport.writeElement((sw) => {
              sw.writeU16(elem_fport);
            });
          }
          seq_fport.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: GpioPinsFields } {
    return deserializeMessage<GpioPinsFields>(bytes, (r) => ({
      port: (() => {
        const seq = r.beginSequence();
        const out: number[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => sr.readU16()));
        }
        return out;
      })(),
    }));
  }
}

export const ID_TOPIC = 'blueberry/devices/{device_type}/{nid}/id';

export interface IdFields {
  id: number;
}

export class IdMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x8ca5;
  static readonly TOPIC_TEMPLATE = ID_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = IdMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: IdFields): Uint8Array {
    return serializeMessage(fields, IdMessage.MODULE_KEY, IdMessage.MESSAGE_KEY, (w, f) => {
      w.writeU32(f.id);
    });
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: IdFields } {
    return deserializeMessage<IdFields>(bytes, (r) => ({
      id: r.readU32(),
    }));
  }
}

export interface Coordinate {
  x: number;
  y: number;
  z: number;
  unit: Unit;
  time: bigint;
}

export type Coordinates = Coordinate[];

export const IMU_DATA_TOPIC = 'blueberry/devices/{device_type}/{nid}/imu-data';

export interface ImuDataFields {
  coords: Coordinate[];
}

export class ImuDataMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x65a1;
  static readonly TOPIC_TEMPLATE = IMU_DATA_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = ImuDataMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: ImuDataFields): Uint8Array {
    return serializeMessage(
      fields,
      ImuDataMessage.MODULE_KEY,
      ImuDataMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fcoords = w.beginSequence();
          for (const elem_fcoords of f.coords ?? []) {
            seq_fcoords.writeElement((sw) => {
              {
                sw.writeF32(elem_fcoords.x);
                sw.writeF32(elem_fcoords.y);
                sw.writeF32(elem_fcoords.z);
                sw.writeU8(elem_fcoords.unit as number);
                sw.writeU64(elem_fcoords.time);
              }
            });
          }
          seq_fcoords.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: ImuDataFields } {
    return deserializeMessage<ImuDataFields>(bytes, (r) => ({
      coords: (() => {
        const seq = r.beginSequence();
        const out: Coordinate[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(
            seq.readElement((sr) => ({
              x: sr.readF32(),
              y: sr.readF32(),
              z: sr.readF32(),
              unit: sr.readU8() as Unit,
              time: sr.readU64(),
            })),
          );
        }
        return out;
      })(),
    }));
  }
}

export enum TriggerPosEnum {
  FIRST_QUARTER_TRIG_POS = 0,
  SECOND_QUARTER_TRIG_POS = 1,
  THIRD_QUARTER_TRIG_POS = 2,
  FOURTH_QUARTER_TRIG_POS = 3,
}

export const OSCOPE_CONFIG_TOPIC = 'blueberry/devices/{device_type}/{nid}/oscope-config';

export interface OscopeConfigFields {
  period: number;
  triggerSource: number;
  trigPos: TriggerPosEnum;
  risingNotFalling: boolean;
  threshold: number;
  holdoff: number;
}

export class OscopeConfigMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x7e1d;
  static readonly TOPIC_TEMPLATE = OSCOPE_CONFIG_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = OscopeConfigMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: OscopeConfigFields): Uint8Array {
    return serializeMessage(
      fields,
      OscopeConfigMessage.MODULE_KEY,
      OscopeConfigMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeF32(f.period);
        w.writeU8(f.triggerSource);
        w.writeU8(f.trigPos as number);
        w.writeBool(f.risingNotFalling);
        w.writeF32(f.threshold);
        w.writeF32(f.holdoff);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: OscopeConfigFields } {
    return deserializeMessage<OscopeConfigFields>(bytes, (r) => ({
      period: r.readF32(),
      triggerSource: r.readU8(),
      trigPos: r.readU8() as TriggerPosEnum,
      risingNotFalling: r.readBool(),
      threshold: r.readF32(),
      holdoff: r.readF32(),
    }));
  }
}

export type OscopeDataSequence = number[];

export const OSCOPE_DATA_TOPIC = 'blueberry/devices/{device_type}/{nid}/oscope-data';

export interface OscopeDataFields {
  channel: number;
  source: number;
  data: number[];
  captureId: number;
  gain: number;
  offset: number;
}

export class OscopeDataMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x831d;
  static readonly TOPIC_TEMPLATE = OSCOPE_DATA_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = OscopeDataMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: OscopeDataFields): Uint8Array {
    return serializeMessage(
      fields,
      OscopeDataMessage.MODULE_KEY,
      OscopeDataMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeU8(f.channel);
        w.writeU8(f.source);
        {
          const seq_fdata = w.beginSequence();
          for (const elem_fdata of f.data ?? []) {
            seq_fdata.writeElement((sw) => {
              sw.writeU8(elem_fdata);
            });
          }
          seq_fdata.end();
        }
        w.writeU32(f.captureId);
        w.writeF32(f.gain);
        w.writeF32(f.offset);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: OscopeDataFields } {
    return deserializeMessage<OscopeDataFields>(bytes, (r) => ({
      channel: r.readU8(),
      source: r.readU8(),
      data: (() => {
        const seq = r.beginSequence();
        const out: number[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => sr.readU8()));
        }
        return out;
      })(),
      captureId: r.readU32(),
      gain: r.readF32(),
      offset: r.readF32(),
    }));
  }
}

export enum AScanType {
  UNDEFINED = 0,
  RAW_RF = 1,
  IQ_DEMOD = 2,
  RECTIFIER_DEMOD = 3,
}

export const SONAR_A_SCAN_TOPIC = 'blueberry/devices/{device_type}/{nid}/sonar-a-scan';

export interface SonarAScanFields {
  captureId: number;
  firstSampleIndex: number;
  totalSampleNum: number;
  sampleRate: number;
  data: number[];
  type_: AScanType;
}

export class SonarAScanMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xc136;
  static readonly TOPIC_TEMPLATE = SONAR_A_SCAN_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = SonarAScanMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: SonarAScanFields): Uint8Array {
    return serializeMessage(
      fields,
      SonarAScanMessage.MODULE_KEY,
      SonarAScanMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeU32(f.captureId);
        w.writeU32(f.firstSampleIndex);
        w.writeU32(f.totalSampleNum);
        w.writeF32(f.sampleRate);
        {
          const seq_fdata = w.beginSequence();
          for (const elem_fdata of f.data ?? []) {
            seq_fdata.writeElement((sw) => {
              sw.writeF32(elem_fdata);
            });
          }
          seq_fdata.end();
        }
        w.writeU8(f.type_ as number);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: SonarAScanFields } {
    return deserializeMessage<SonarAScanFields>(bytes, (r) => ({
      captureId: r.readU32(),
      firstSampleIndex: r.readU32(),
      totalSampleNum: r.readU32(),
      sampleRate: r.readF32(),
      data: (() => {
        const seq = r.beginSequence();
        const out: number[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => sr.readF32()));
        }
        return out;
      })(),
      type_: r.readU8() as AScanType,
    }));
  }
}

export enum Parameter8 {
  REVID = 0,
  ID = 1,
}

export enum Parameter16 {
  CLEAR = 0,
  RED = 1,
  GREEN = 2,
  BLUE = 3,
}

export const TCS3400_DATA8_TOPIC = 'blueberry/devices/{device_type}/{nid}/tcs3400-data8';

export interface Tcs3400Data8Fields {
  param: Parameter8;
  data: number;
}

export class Tcs3400Data8Message {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xc342;
  static readonly TOPIC_TEMPLATE = TCS3400_DATA8_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = Tcs3400Data8Message.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: Tcs3400Data8Fields): Uint8Array {
    return serializeMessage(
      fields,
      Tcs3400Data8Message.MODULE_KEY,
      Tcs3400Data8Message.MESSAGE_KEY,
      (w, f) => {
        w.writeU8(f.param as number);
        w.writeU8(f.data);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: Tcs3400Data8Fields } {
    return deserializeMessage<Tcs3400Data8Fields>(bytes, (r) => ({
      param: r.readU8() as Parameter8,
      data: r.readU8(),
    }));
  }
}

export const TCS3400_DATA16_TOPIC = 'blueberry/devices/{device_type}/{nid}/tcs3400-data16';

export interface Tcs3400Data16Fields {
  param: Parameter16;
  data: number;
}

export class Tcs3400Data16Message {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x6efe;
  static readonly TOPIC_TEMPLATE = TCS3400_DATA16_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = Tcs3400Data16Message.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: Tcs3400Data16Fields): Uint8Array {
    return serializeMessage(
      fields,
      Tcs3400Data16Message.MODULE_KEY,
      Tcs3400Data16Message.MESSAGE_KEY,
      (w, f) => {
        w.writeU8(f.param as number);
        w.writeU16(f.data);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: Tcs3400Data16Fields } {
    return deserializeMessage<Tcs3400Data16Fields>(bytes, (r) => ({
      param: r.readU8() as Parameter16,
      data: r.readU16(),
    }));
  }
}

export interface ThermistorConfigStruct {
  channel: number;
  enabled: boolean;
  adcChannel: number;
  c0: number;
  c1: number;
  c2: number;
  c3: number;
}

export type ThermistorConfigSequence = ThermistorConfigStruct[];

export const THERMISTOR_CONFIG_TOPIC = 'blueberry/devices/{device_type}/{nid}/thermistor-config';

export interface ThermistorConfigFields {
  config: ThermistorConfigStruct[];
}

export class ThermistorConfigMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xaae3;
  static readonly TOPIC_TEMPLATE = THERMISTOR_CONFIG_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = ThermistorConfigMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: ThermistorConfigFields): Uint8Array {
    return serializeMessage(
      fields,
      ThermistorConfigMessage.MODULE_KEY,
      ThermistorConfigMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fconfig = w.beginSequence();
          for (const elem_fconfig of f.config ?? []) {
            seq_fconfig.writeElement((sw) => {
              {
                sw.writeU8(elem_fconfig.channel);
                sw.writeBool(elem_fconfig.enabled);
                sw.writeU8(elem_fconfig.adcChannel);
                sw.writeF32(elem_fconfig.c0);
                sw.writeF32(elem_fconfig.c1);
                sw.writeF32(elem_fconfig.c2);
                sw.writeF32(elem_fconfig.c3);
              }
            });
          }
          seq_fconfig.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: ThermistorConfigFields } {
    return deserializeMessage<ThermistorConfigFields>(bytes, (r) => ({
      config: (() => {
        const seq = r.beginSequence();
        const out: ThermistorConfigStruct[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(
            seq.readElement((sr) => ({
              channel: sr.readU8(),
              enabled: sr.readBool(),
              adcChannel: sr.readU8(),
              c0: sr.readF32(),
              c1: sr.readF32(),
              c2: sr.readF32(),
              c3: sr.readF32(),
            })),
          );
        }
        return out;
      })(),
    }));
  }
}

export interface ThermistorDataStruct {
  ch: number;
  data: number;
}

export type ThermistorDataSequence = ThermistorDataStruct[];

export const THERMISTOR_DATA_TOPIC = 'blueberry/devices/{device_type}/{nid}/thermistor-data';

export interface ThermistorDataFields {
  data: ThermistorDataStruct[];
}

export class ThermistorDataMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0xd51a;
  static readonly TOPIC_TEMPLATE = THERMISTOR_DATA_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = ThermistorDataMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: ThermistorDataFields): Uint8Array {
    return serializeMessage(
      fields,
      ThermistorDataMessage.MODULE_KEY,
      ThermistorDataMessage.MESSAGE_KEY,
      (w, f) => {
        {
          const seq_fdata = w.beginSequence();
          for (const elem_fdata of f.data ?? []) {
            seq_fdata.writeElement((sw) => {
              {
                sw.writeU8(elem_fdata.ch);
                sw.writeF32(elem_fdata.data);
              }
            });
          }
          seq_fdata.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: ThermistorDataFields } {
    return deserializeMessage<ThermistorDataFields>(bytes, (r) => ({
      data: (() => {
        const seq = r.beginSequence();
        const out: ThermistorDataStruct[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => ({ ch: sr.readU8(), data: sr.readF32() })));
        }
        return out;
      })(),
    }));
  }
}

export type Time64 = bigint;

export const TIME_TOPIC = 'blueberry/devices/{device_type}/{nid}/time';

export interface TimeFields {
  hostTime: number;
  localTime: number;
}

export class TimeMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x3c5b;
  static readonly TOPIC_TEMPLATE = TIME_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = TimeMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: TimeFields): Uint8Array {
    return serializeMessage(fields, TimeMessage.MODULE_KEY, TimeMessage.MESSAGE_KEY, (w, f) => {
      w.writeU32(f.hostTime);
      w.writeU32(f.localTime);
    });
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: TimeFields } {
    return deserializeMessage<TimeFields>(bytes, (r) => ({
      hostTime: r.readU32(),
      localTime: r.readU32(),
    }));
  }
}

export const TIME_SYNC_TOPIC = 'blueberry/devices/{device_type}/{nid}/timesync';

export interface TimeSyncFields {
  senderTime: bigint;
  lastReceivedTime: bigint;
}

export class TimeSyncMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x767d;
  static readonly TOPIC_TEMPLATE = TIME_SYNC_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = TimeSyncMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: TimeSyncFields): Uint8Array {
    return serializeMessage(
      fields,
      TimeSyncMessage.MODULE_KEY,
      TimeSyncMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeU64(f.senderTime);
        w.writeU64(f.lastReceivedTime);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: TimeSyncFields } {
    return deserializeMessage<TimeSyncFields>(bytes, (r) => ({
      senderTime: r.readU64(),
      lastReceivedTime: r.readU64(),
    }));
  }
}

export enum HwType {
  UNDEFINED = 65535,
  LEGACY = 0,
  BLUE_SERVO = 1,
  LUMEN = 2,
  NUCLEO = 3,
  BLUE_ESC = 4,
  GIGABOARD = 5,
  BLUE_BRIDGE = 6,
  POGOBRAIN = 7,
}

export enum McuType {
  UNDEFINED = 255,
  STM32F446 = 1,
  STM32H563 = 2,
  STM32H573 = 3,
  STM32G071 = 4,
}

export const VERSION_TOPIC = 'blueberry/devices/{device_type}/{nid}/version';

export interface VersionFields {
  firmwareVersion: number;
  hardwareRev: number;
  mcuType: McuType;
  hardwareType: HwType;
}

export class VersionMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x8366;
  static readonly TOPIC_TEMPLATE = VERSION_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = VersionMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: VersionFields): Uint8Array {
    return serializeMessage(
      fields,
      VersionMessage.MODULE_KEY,
      VersionMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeU32(f.firmwareVersion);
        w.writeU8(f.hardwareRev);
        w.writeU8(f.mcuType as number);
        w.writeU16(f.hardwareType as number);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: VersionFields } {
    return deserializeMessage<VersionFields>(bytes, (r) => ({
      firmwareVersion: r.readU32(),
      hardwareRev: r.readU8(),
      mcuType: r.readU8() as McuType,
      hardwareType: r.readU16() as HwType,
    }));
  }
}

export const WHOS_THERE_TOPIC = 'blueberry/devices/{device_type}/{nid}/whos-there';

export interface WhosThereFields {
  probability: number;
  time: number;
}

export class WhosThereMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x1971;
  static readonly TOPIC_TEMPLATE = WHOS_THERE_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = WhosThereMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: WhosThereFields): Uint8Array {
    return serializeMessage(
      fields,
      WhosThereMessage.MODULE_KEY,
      WhosThereMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeF32(f.probability);
        w.writeF32(f.time);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: WhosThereFields } {
    return deserializeMessage<WhosThereFields>(bytes, (r) => ({
      probability: r.readF32(),
      time: r.readF32(),
    }));
  }
}

export enum I2cDevBb {
  I2CNULL_DEV = 255,
  I2C1_DEV = 0,
  I2C2_DEV = 1,
  I2C3_DEV = 2,
  I2C4_DEV = 3,
}

export type I2cTransactionData = number[];

export const I2C_TRANSACTION_TOPIC = 'blueberry/devices/{device_type}/{nid}/i2c-transaction';

export interface I2cTransactionFields {
  i2cDev: I2cDevBb;
  i2cAddr: number;
  numTxBytes: number;
  numRxBytes: number;
  transactionData: number[];
  transactionId: number;
}

export class I2cTransactionMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x35b1;
  static readonly TOPIC_TEMPLATE = I2C_TRANSACTION_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = I2cTransactionMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: I2cTransactionFields): Uint8Array {
    return serializeMessage(
      fields,
      I2cTransactionMessage.MODULE_KEY,
      I2cTransactionMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeU8(f.i2cDev as number);
        w.writeU8(f.i2cAddr);
        w.writeU16(f.numTxBytes);
        w.writeU16(f.numRxBytes);
        {
          const seq_ftransac = w.beginSequence();
          for (const elem_ftransac of f.transactionData ?? []) {
            seq_ftransac.writeElement((sw) => {
              sw.writeU32(elem_ftransac);
            });
          }
          seq_ftransac.end();
        }
        w.writeI32(f.transactionId);
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: I2cTransactionFields } {
    return deserializeMessage<I2cTransactionFields>(bytes, (r) => ({
      i2cDev: r.readU8() as I2cDevBb,
      i2cAddr: r.readU8(),
      numTxBytes: r.readU16(),
      numRxBytes: r.readU16(),
      transactionData: (() => {
        const seq = r.beginSequence();
        const out: number[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => sr.readU32()));
        }
        return out;
      })(),
      transactionId: r.readI32(),
    }));
  }
}

export enum ClockSpecBb {
  SPI_CPOL_0_CPHA_0 = 0,
  SPI_CPOL_0_CPHA_1 = 1,
  SPI_CPOL_1_CPHA_0 = 2,
  SPI_CPOL_1_CPHA_1 = 3,
}

export enum SpiDevBb {
  SPINULL_DEV = 255,
  SPI1_DEV = 0,
  SPI2_DEV = 1,
  SPI3_DEV = 2,
  SPI4_DEV = 3,
  SPI5_DEV = 4,
}

export enum BaudDivBb {
  SPI_DIV_2 = 0,
  SPI_DIV_4 = 1,
  SPI_DIV_8 = 2,
  SPI_DIV_16 = 3,
  SPI_DIV_32 = 4,
  SPI_DIV_64 = 5,
  SPI_DIV_128 = 6,
  SPI_DIV_256 = 7,
}

export type SpiTransactionData = number[];

export const SPI_TRANSACTION_TOPIC = 'blueberry/devices/{device_type}/{nid}/spi-transaction';

export interface SpiTransactionFields {
  spiDev: SpiDevBb;
  csPin: PortPinEnum;
  baudDiv: BaudDivBb;
  clockSpec: ClockSpecBb;
  numBytes: number;
  deAssertCs: boolean;
  transactionId: number;
  transactionData: number[];
}

export class SpiTransactionMessage {
  static readonly MODULE_KEY = 0x4244;
  static readonly MESSAGE_KEY = 0x25ed;
  static readonly TOPIC_TEMPLATE = SPI_TRANSACTION_TOPIC;

  static topic(params: { deviceType: string; nid: string }): string {
    let out = SpiTransactionMessage.TOPIC_TEMPLATE as string;
    out = out.split('{device_type}').join(params.deviceType);
    out = out.split('{nid}').join(params.nid);
    return out;
  }

  static encode(fields: SpiTransactionFields): Uint8Array {
    return serializeMessage(
      fields,
      SpiTransactionMessage.MODULE_KEY,
      SpiTransactionMessage.MESSAGE_KEY,
      (w, f) => {
        w.writeU8(f.spiDev as number);
        w.writeU8(f.csPin as number);
        w.writeU8(f.baudDiv as number);
        w.writeU8(f.clockSpec as number);
        w.writeU16(f.numBytes);
        w.writeBool(f.deAssertCs);
        w.writeI32(f.transactionId);
        {
          const seq_ftransac = w.beginSequence();
          for (const elem_ftransac of f.transactionData ?? []) {
            seq_ftransac.writeElement((sw) => {
              sw.writeU32(elem_ftransac);
            });
          }
          seq_ftransac.end();
        }
      },
    );
  }

  static decode(bytes: Uint8Array): { header: MessageHeader; fields: SpiTransactionFields } {
    return deserializeMessage<SpiTransactionFields>(bytes, (r) => ({
      spiDev: r.readU8() as SpiDevBb,
      csPin: r.readU8() as PortPinEnum,
      baudDiv: r.readU8() as BaudDivBb,
      clockSpec: r.readU8() as ClockSpecBb,
      numBytes: r.readU16(),
      deAssertCs: r.readBool(),
      transactionId: r.readI32(),
      transactionData: (() => {
        const seq = r.beginSequence();
        const out: number[] = [];
        for (let i = 0; i < seq.count; i++) {
          out.push(seq.readElement((sr) => sr.readU32()));
        }
        return out;
      })(),
    }));
  }
}
